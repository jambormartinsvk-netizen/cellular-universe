# Auditný skript 20: surové rezíduá H0

`20_script_H0_raw_residual_audit.py` počíta iba jednorozmerné aritmetické rozdiely bodov pipeline voči SH0ES a DESI DR2+CMB kotvám.

Výsledky nesmú byť interpretované ako modelová likelihood alebo konečná významnosť Hubbleho napätia.

Referenčné spustenie:

```powershell
python scripts/20_script_H0_raw_residual_audit.py
```

