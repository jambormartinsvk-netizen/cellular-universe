# REGISTER 05 — EN update of Q20 after A2-K5.1

**Date:** 2026-07-13  
**New rules:** none

## Q20 — current status

- A2-K1: `DEAD M-009` — superhorizon mode proportional to `2 Gamma/delta`.
- A2-K2: `DEAD M-008` — `c_s^2=w<0`.
- A2-K3: `DEAD M-010` — superhorizon mode proportional to `Gamma/delta`.
- A2-K4: `DEAD M-011` — `det M<0`, physical relative eigenmode.
- A2-K5/K1: `SURVIVES A2-K5.1 — 60/100`; the relative and adiabatic
  superhorizon tests pass, while the A3 growth gate remains red.

K5/K1 uses the fundamental number-density contrast `delta_n` and the identity
`delta_c=delta_n+beta delta varphi`. It contains no fluid pole
`1/(1+w_f)`. The full test gives a coupled/null relative-mode gain of
`0.4748966` and a global 00 residual of `1.4754e-9`. The adiabatic mode
converges to zero relative velocity.

The quasi-static cross-check also confirms a strong attractive force,
`G_eff/G=5.57–5.67` on the tested present-day scales. The track is therefore
not observationally approved.

## New/refined tracks after the failure analysis

1. A2-K5/K3a — an `f(n_c,phi,X,Z)` energy+momentum action targeting
   `G_eff<=G`;
2. A2-K5/K4a — a finite-enthalpy mediator;
3. A2-K5/K2a — number production of constant-mass ash;
4. A2-K5/K6 — production and elastic momentum transfer tied by one operator;
5. A1-K2/A2-K6a — thresholded transfer changing the background and possibly
   requiring v4.

The existing register rules are neither changed nor duplicated. Full failure
analysis: `Audit/A2_analyza_hlavnych_pricin_smrti_kolaji.md`.
