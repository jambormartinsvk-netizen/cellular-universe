# 05 — Methodology and Question Register Addendum: K4/BR3B-2g (EN)

Date: 2026-07-14

This addendum does not alter existing rules.

## AR42 — A higher multipole must obey gradient-generated regularity

After rescaled higher multipoles are introduced, a coefficient system can
contain mathematically regular homogeneous solutions which, after undoing the
rescaling, correspond to a non-zero early multipole without a source from the
lower hierarchy. Such a mode is not an allowed initial condition of the
standard Boltzmann hierarchy.

Before a rank or physical verdict, every higher multipole below its first
gradient-generated order must be set explicitly to zero. Only then do the
rank, null direction, and regression against earlier coefficients have a
physical interpretation.

If missing regularity contaminates earlier coefficients, the result is
`REVIEW_UNCLOSED`, not track death. The original script is retained with the
reason, and the correction is made in a new numbered clone.

## Q69 — Has K4 passed the first `l=3` and gravitating-ash sector?

**Status: YES; K4 ALIVE, G7 OPEN.**

After regularity was imposed, script 127 passed `40/40` checks at truncation
orders 5 and 6. The NID/NIV physical matrices have rank `66/66`; the
lambda-zero common coefficients reproduce script 124 at `~10^-16`. Script
128 independently passed `16/16` exact identities.

The first `L3` feedback and ash `delta_c` occur at NID `p+4` and NIV `p+3`.
The ash/CDM stress gravitates at NID `p+5` and NIV `p+4`. It is non-zero but
small.

Passing BR3B-2g does not close all of G7. The BR3C two-depth evolution and all
four Einstein-constraint audits remain. The score stays `60/100 = G6`.

