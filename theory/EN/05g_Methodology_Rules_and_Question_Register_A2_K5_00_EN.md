# REGISTER 05 — EN update of Q20 after A2-K5.0

**Date:** 2026-07-13  
**New rules:** none

## Q20 — A2-K5 split into microphysical tracks

- A2-K1 through A2-K4 remain dead M-009, M-008, M-010, and M-011.
- A2-K5/K1: canonical scalar fuel and conformally coupled CDM;
  `SURVIVES K5.0 ONLY — 45/100`, with a red growth gate.
- A2-K5/K2: explicit particle decay; waiting.
- A2-K5/K3: k-essence/current action; waiting.
- A2-K5/K4: explicit dynamical mediator; waiting.
- A2-K5/K5: elastic/topological cell-network action; waiting and may require v4.

No new Q21 is created; these are nested tracks of the existing Q20.

## K5/K1 result

The local action exactly reconstructs `Q=Gamma rho_f`, has the correct
canonical kinetic sign, `c_s^2=1`, and positive reconstructed background
`m_phi^2` and `m_eff^2`. The `lambda->0` limit at fixed `delta>0` is regular.
The `delta->0` limit at fixed `lambda` is singular because
`beta proportional lambda/sqrt(delta)`.

The quasi-static test enhances weighted matter growth by `5.20–5.30%`; a
diagnostic projection moves the internal `S8` value to about `0.920`. This is
not a full CMB-normalized prediction, so M-012 is not issued yet. The next
step is the fully relativistic A2-K5.1 system and then CLASS/CAMB.

## Older formulations restricted by the later audit

- A1-K1 is background bookkeeping, not a living perturbation theory.
- K5/K1 does not resurrect dead A2-K1 M-009.
- the old ad hoc drag cannot be quoted without the fifth force generated by
  the same coupling;
- `S8=0.8745` is not the final K5/K1 prediction;
- in K5/K1, ash is not non-interacting after birth: `m_c=m_c(phi)`.

Full audit: `Audit/A2_K5_00_canonical_scalar_action_reconstruction_and_growth_risk.md`.
