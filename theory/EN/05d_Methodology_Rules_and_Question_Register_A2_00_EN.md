# REGISTER 05 — EN update of Q20 after A2.0

**Date:** 2026-07-13  
**Follows:** `05c_Methodology_Rules_and_Question_Register_v3.18_ADDENDUM_EN.md`  
**New rules:** none; AR1–AR7 are unchanged

## Q20 — updated status

**Status:** **ACTIVE; A2.0 PASSED IN THE EFFECTIVE BALANCE SCOPE.**

Active track A2-K1 uses

`Q^mu=Gamma rho_f u_c^mu`, `c_s,f^2=1`, `pi_f=0`.

The total transfer cancels pairwise, CDM has no momentum transfer in its own frame, and the FRW limit exactly recovers A1-K1. `Gamma` must be a constant local scalar; `lambda=Gamma/H0` is only a parametrization. The microphysical origin of `Gamma` and of the `c_s,f^2=1` closure has not been derived.

**Next:** A2.1 — complete linear continuity and Euler equations, metric constraints, and a model-specific superhorizon test.

## M-008 — A2-K2

**Status:** **DEAD — ARCHIVED.**

The strictly barotropic closure gives `c_s,f^2=w_f=-0.97703<0`, hence the exponential gradient instability `delta_f proportional exp(|c_s|k eta)`. The reason, script, and output are preserved in:

- `Audit/A2_K2_MRTVA_barotropicke_palivo_gradientova_nestabilita.md`;
- `scripts/21_script_A2_barotropic_fuel_gradient_instability.py`;
- `scripts/README_AUDIT_SCRIPT_21.md`.

The verdict does not kill A2-K1 with `c_s,f^2=1`.

