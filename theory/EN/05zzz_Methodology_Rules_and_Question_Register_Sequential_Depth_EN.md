# Addendum to 05 — uniform sequential track depth (EN)

**Date:** 2026-07-14  
**Restricts:** AR14  
**Does not change:** physical death reasons or their scope

## Duplication check

AR14 correctly separated score from probability of truth, but did not state
whether the “furthest documented gate” had to pass or whether all
intermediate gates had to pass. AR30 adds exactly this missing sequential
condition and does not duplicate AR14.

## AR30 — A score is only the highest sequentially passed canonical gate

Every track uses the same G1–G10 gates and scores in steps of ten. An
`N/100` score is awarded only when that gate and every preceding gate (or an
explicitly proven equivalent) have passed.

A later no-go, screen, or kill test:

- may physically kill the track;
- is recorded as the `deepest executed test / death gate`;
- does not increase the canonical score when intermediate gates were skipped;
- remains archived with its script, output, and death reason.

Historical intermediate scores `25`, `32–42`, `45`, `55`, `59`, and `75`
are not deleted. Current tables retain them only as historical checkpoints or
deepest executed tests, not as comparable canonical depths.

## Canonical gates

| Gate | Score | Content |
|---|---:|---|
| G1 | 10 | registered distinct hypothesis |
| G2 | 20 | closed A1 background and ledger |
| G3 | 30 | local action or complete covariant closure |
| G4 | 40 | complete linear equations and Einstein constraints |
| G5 | 50 | complete regular superhorizon basis |
| G6 | 60 | high-k/subhorizon stability of the complete basis |
| G7 | 70 | own full Einstein–Boltzmann implementation and physical transfers |
| G8 | 80 | CMB-normalised spectra and `sigma8/S8` |
| G9 | 90 | joint likelihood and systematics |
| G10 | 100 | every version gate and independent reproduction |

## Q57 — Why does K4 score 60/100 after K4.2, and what is still missing?

**Status:** `K4 IS THE DEEPEST SEQUENTIALLY PASSED A2 TRACK; G7 IS OPEN.`

K4.1 passed G5 with the complete regular basis and K4.2 passed G6 high-k
and subhorizon stability. Its historical `59/100` is therefore uniformly
recalibrated to `60/100`.

K4 is not missing one equation. G7 requires the complete package:

1. photon and neutrino Boltzmann hierarchies;
2. anisotropic stress and `Phi != Psi`;
3. tight coupling and recombination;
4. complete regular initial modes of the enlarged system;
5. the unchanged K4 operator without a new post-data parameter;
6. constraint, null, gauge/independent-code, and convergence tests;
7. physical transfer functions `delta_m(k,z)`.

Only the complete G7 awards `70/100` and entry into A3/G8. CMB
normalisation and `S8` belong to G8, not to the current score of 60.

## Recalibration result

Canonical K1–K12 scores are recorded in
`Audit/JEDNOTNA_SEKVENCNA_STUPNICA_HLBKY_A2_A_REKALIBRACIA_K1_K12.md`.
Physical verdicts M-008 through M-016 are unchanged by this recalibration.

